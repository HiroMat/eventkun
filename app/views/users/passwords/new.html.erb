<h2>パスワードの再発行</h2>

<%= form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :post }) do |f| %>
  <%= devise_error_messages! %>

  <div class="field">
    <%= f.label :メールアドレス %><br />
    <%= f.email_field :email, autofocus: true %>
  </div>
  <br>
  <div class="actions">
    <%= f.submit "パスワード再発行のメールを上記アドレスに送信" %>
  </div>
  <br>
<% end %>

<%= render "users/shared/links" %>
